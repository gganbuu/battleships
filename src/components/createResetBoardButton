export const createResetBoardButton = () => {
    const button = document.createElement("button")
    button.classList.add('resetboard-button')
    button.textContent = "reset board"
    return button
}